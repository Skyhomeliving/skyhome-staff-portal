# Test-run environment. Keeps the suite off the real data directory: importing
# auth.js pulls in db.js, which opens (and migrates) whatever DATA_DIR points at.
# Without this, `npm test` would run migrations against ./data.
DATA_DIR=./.test-data
NODE_ENV=test
